{
  "name": "babycare",
  "version": "1.0.0",
  "description": "Sistema de cuidados para bebês",
  "main": "index.js",
  "scripts": {
    "start": "npx json-server --watch db.json --port 3000",
    "dev": "live-server --port=8080"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
